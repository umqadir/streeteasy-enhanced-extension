# Legacy Scripts

These scripts are preserved for historical scraping/debug workflows and are **not** part of the current recommended CV dataset pipeline.

Use the active scripts in `sample-collection/scripts/` instead:

- `curate_web.py`

If you need one-off recovery from older collection runs, use legacy scripts cautiously and avoid editing canonical datasets in place.
